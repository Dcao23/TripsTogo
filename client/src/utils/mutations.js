import { gql } from '@apollo/client';

export const CREATE_TRIP = gql`
  mutation createTrip($Destination: String!, $Shared Experience: String!) {
      Destination
      Shared Experience
    }
  }
`;
